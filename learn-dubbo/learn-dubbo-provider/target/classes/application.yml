# 定义 Dubbo 的应用名、Dubbo 协议信息、Dubbo 使用的注册中心地址。
dubbo:
  application:
    name: dubbo-springboot-demo-provider
  protocol:
    name: dubbo
    port: -1
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
